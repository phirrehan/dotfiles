# Termux
if [ -n "$TERMUX_VERSION" ]; then
  alias i="nala update && nala install $1"
  alias rem="nala remove $1"
  alias s="nala search $1" alias u="nala update && nala upgrade"
  alias c="clear"
else
  pacf() {
    pacman -Slq | fzf --multi --preview 'pacman -Si {1}' \
    --preview-window=wrap,border-sharp | xargs -ro sudo pacman -S
  }
  pacrm() {
    pacman -Qq | fzf --multi --preview 'pacman -Qi {1}' \
    --preview-window=wrap,border-sharp | xargs -ro sudo pacman -Rns
  }
  paruf() {
    yay -Slq | fzf --multi --preview 'yay -Si {1}' \
    --preview-window=wrap,border-sharp | xargs -ro paru -S
  }

fi

# System
alias ls="ls --color=auto"
alias Ls="ls -A --color=auto"
alias grep="grep --color=auto"
alias change="cat ~/.local/state/caelestia/sequences.txt"

# Zsh Config
alias cfz="cd ~/.config/zsh/ && nvim"
alias cfh="cd ~/.config/hypr/ && nvim"
alias cfq="cd ~/.config/caelestia/ && nvim"
alias svim="sudo -E nvim"
alias sz="source ~/.zshrc"

# Nvim
# alias vim="nvim"
